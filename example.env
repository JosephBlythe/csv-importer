# Application Environment
APP_ENV=development   # 'development' for local dev, 'production' for production

# Database Configuration
DB_HOST=localhost      # Use 'localhost' for local development, 'db' for Docker
DB_PORT=5432          # Default PostgreSQL port
DB_NAME=my_importer   # Your database name
DB_USER=my_user       # Your database username
DB_PASSWORD=secret    # Your database password (change this!)
